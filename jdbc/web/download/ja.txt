java选择题
 1，类中实现方法重载的方式是：采用不同的形式参数列表
2，RandomAccessFile类同时实现了DataInput和DataOutput接口
3，派生出大多数Swing组件的类是：Container
4,返回字符数组的字符输入流类是：CharArrayReader
5，在java异常处理中，无论是否发生异常，其后的代码块都会被 执行关键字是：finally
6，对于长度为n的有序线性表，在最坏的情况下，二分法查找只需要比较log2n次，而顺序查找需要比较n次
7，链式存储结构既可以针对线性结构也可以针对非线性结构，
8，链式存储结构中每个结点都由数据域与指针域两个部分组成，增加了存储空间
9，数据流图中带有箭头的线段表示的是数据流
10，在软件开发中，需求分析阶段可以使用的工具有数据流图DFD图，数据字典DD，判定树与判定表
11，对象有如下一些基本特点，标识唯一性，分类性，多态性，封装性，模块独立性好
12，在数据库管理技术发展的三个阶段中，数据库系统阶段结构简单，使用方便逻辑性强物理性少，数据共享最好
13，自然连接，比较相同的属性组，并且在结果中吧重复的属性列去掉，
14，运算符中，优先级最高的是--
15，while（true）{}可以实现无限循环
16，HashMap不能够保证其中元素的顺序
17，PipedInputStream类是实现线程之间通信的类
18，处理鼠标进入窗口时所产生的鼠标事件的方法 是：mouseEntered(MouseEvent)
19,JOptionPane不能实现的对话框是：通用对话框
10，栈是先进后出的线性表，队列是先进先出的线性表，循环队列是线性结构的线性表。
11，支持子程序调用的数据结构是栈。
12，栈是一种只能在一端进行插入或删除的线性表





